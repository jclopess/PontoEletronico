spring.application.name=registroponto

# Configuração do DataSource para PostgreSQL (ajustada para Docker)
spring.datasource.url=jdbc:postgresql://db:5432/registroponto_db
spring.datasource.username=postgres
spring.datasource.password=H^062050010201ow

# Configuração do Hibernate (parte do JPA)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configuração da porta do servidor
server.port=8081

